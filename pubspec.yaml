name: downlow
description: Easy to use Http Downloader (Supports continue file download, cancellation and more).
version: 0.1.0

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  http: ^0.12.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
